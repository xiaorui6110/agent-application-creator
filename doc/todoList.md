## TodoList

> xiaorui

### 目前概览

总体的 ReactAgent 已经基本上构成，其中有部分 Tool、Hook 和 Interceptor 的实际逻辑细节未实现。
其次还有长期记忆存储需要完成，初步想法是缓存 Redis + 持久化存储 MongoDB（MySQL 尽量做一下适配）。
然后，实际的 agent 调用方法服务还未封装完成，之后需要多打 log 和 debug 深入理解一下底层的数据传递过程。


### 1. 先完成模型记忆的持久化存储

### 2. 再将服务封装完成

### 3. 最后将工具调用和一些钩子等补充逻辑