# 目前需要继续完善优化的点

> 主要是对后端已经实现的接口，但是前端并未使用或者展示页面功能的点，进行优化完善

## 用户模块

1. 用户封禁功能： `/ban` 没有在前端的用户管理界面使用
2. 用户批量删除： `/delete/batch` 好像没有在前端使用（我有点记忆模糊，最好检查一下）
3. 用户退出登录：这点是需要修复的功能，目前在前端使用下来的体验是，用户只要关闭了页面，如果再次访问页面，就会丢失登录态，这样子很降低用户体验，自己的想法是在用户关闭页面后，不清除redis中的token信息，常见的网站都会保存一周时间
4. 用户权限设置：应该需要在用户管理页面，使每个用户的权限信息可以修改，普通用户或管理员，便于平台管理
5. 用户数据报表：这点我还没实现，目前有这样的想法

## 应用模块

1. 应用分类检索：之前我们实现了应用的版本和模板管理，但是没有实现通过分类或应用名称来对应用进行搜索的功能
2. 应用评论优化：之前自己在评论表设计时，预留的父子评论的一些设计，但是前后端还未实现父子评论相关的设计

## Agent 对话模块

1. 手动失败重试：后端已实现手动重试，但前端并未使用，可以在对话页面新增手动重试的功能

...（暂时先这些，后面再进行优化）
